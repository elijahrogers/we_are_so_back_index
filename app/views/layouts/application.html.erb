<!DOCTYPE html>
<html class="scroll-smooth">
  <head>
    <title><%= content_for(:title) || "Wearesobackindex" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen m-0 bg-black text-slate-100 relative">
    <header class="sticky top-0 z-20 border-b border-white/10 bg-black/60 backdrop-blur">
      <div class="mx-auto max-w-7xl px-5 h-14 flex items-center justify-between">
        <div class="flex items-baseline gap-3">
          <a href="/" class="font-semibold tracking-tight text-base sm:text-lg">So Back Index™</a>
        </div>
        <nav class="flex items-center gap-6 text-xs sm:text-sm md:text-base text-slate-300">
          <a href="#huh" class="hover:text-white">Huh</a>
          <a href="#history" class="hover:text-white">History</a>
        </nav>
      </div>
    </header>

    <main class="relative z-10 mx-auto max-w-6xl px-5 py-8">
      <%= yield %>
    </main>

    <footer class="border-t border-white/10 bg-black">
      <div class="mx-auto max-w-7xl px-5 h-12 text-xs sm:text-sm text-slate-400 flex items-center justify-between">
        Not investment advice.

        <%= link_to 'https://github.com/elijahrogers/we_are_so_back_index', class: 'hover:text-white' do %>
          <span>GitHub</span>
        <% end %>
      </div>
    </footer>
  </body>
</html>
